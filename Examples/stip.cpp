///  C++ Example Code
///  ================
///  We first include the `iostream` header file for using the input/output
///  facilities
///   - item 1
///   - item 2
#include <iostream>

///  We import the `std` namespace such that later on we can simply write
///  e.g. `cout` instead of `std::cout`
using namespace std;

///  The function `main` gets called first when you execute your programm.
///  When the function returns an integer value it becomes the exit code of
///  programm.
int
main()
{
    /// In C++ you can use streams for input/output. In the following line
    /// the sting `Hello world!` and the newline character flows into the    +
    /// _*c*_onsole _*out*_put
    // normal comment
    cout << "Hello world!" << endl;
}