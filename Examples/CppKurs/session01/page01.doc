==================
First Example in C                                                  [TOC]
==================

We start with a simple C program defining a max function.

Source Code
===========

--- FILE (file=session01/example01.c) ------------------------------------------
/// Include file _stdio.h_ for later using *printf*
#include <stdio.h>

/// *max* function for arguments of type *double*
double
max(double x, double y)
{
    if (x>y) {
        return x;
    } else {
        return y;
    }
}

/// Function *main* returns the maximum of its two arguments.  Note that the
/// functions expects arguments of type *double*.
int
main()
{
    printf("%f", max(2.2, 2.5));
    return 0;
}
--------------------------------------------------------------------------------

:import: session01/example01.c [stripped, downloadable]

Code Walkthrough
================

:import: session01/example01.c

Compile & Run
=============

--- SHELL ----------------------------------------------------------------------
cd session01
gcc -Wall -o example01 example01.c
./example01
--------------------------------------------------------------------------------
